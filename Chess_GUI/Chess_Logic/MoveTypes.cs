namespace Chess_Logic
{
    // enum of move types
    public enum MoveTypes
    {
        Standard,
        CastlingKingSide,
        CastlingQueenSide,
        PawnStart,
        EnPassant,
        Promotion
    }
}
